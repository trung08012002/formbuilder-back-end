export enum SORT_DIRECTIONS {
  ASCENDING = 'asc',
  DESCENDING = 'desc',
}

export enum DATE_ACTIONS {
  EQUALS = 'eq',
  LESS_THAN = 'lt',
  LESS_THAN_EQUAL = 'lte',
  GREAT_THAN = 'gt',
  GREAT_THAN_EQUAL = 'gte',
}

export enum OTHER_FIELDS_ACTIONS {
  EQUALS = 'eq',
  NOTE_EQUAL = 'not',
  INCLUDES = 'contains',
}
